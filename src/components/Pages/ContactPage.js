import { Base } from "./backgroundPage/image";
import { Form } from "./ContactPageInfo.js";

export const ContactPage = () =>{

    const title = "Contact"

    return(
        <Base title={title} />
    );
};